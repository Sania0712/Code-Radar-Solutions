#include <stdio.h>
#include <math.h>
int main(){

    float r;
    float area;
    area = m_PI * r * r;

    printf("Area: ");
    scanf("%f",&r);

    printf("%.2f",area);
    return 0;
}