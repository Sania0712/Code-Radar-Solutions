#include <stdio.h>
int main(){
    int i=1 , n , product,count;
    printf("");
    scanf("%d",&n);

    while(count<=10){
       n * count = product;
       count = count+1;
       printf("%d * %d = %d",n,count,product);
    }

    return 0;
}